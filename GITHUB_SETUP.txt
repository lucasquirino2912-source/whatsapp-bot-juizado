📖 GUIA PARA IMPORTAR NO GITHUB
================================

Seu projeto está 100% pronto para ser importado no GitHub!

✅ ARQUIVOS CRIADOS:
====================
- .gitignore (configurado para projeto Node.js/WhatsApp)
- README.md (documentação completa)
- DEPLOY_GUIDE.txt (guia de deployment)
- ecosystem.config.js (config PM2)
- .env (variáveis de ambiente)
- start.sh / start.bat (scripts de inicialização)

📋 PASSO A PASSO PARA GITHUB:
=============================

1️⃣ INSTALAR GIT (se não tiver):
   Windows: Baixar em https://git-scm.com/download/win
   Linux: sudo apt-get install git
   Mac: brew install git

2️⃣ CONFIGURAR GIT (primeira vez):
   git config --global user.name "Seu Nome"
   git config --global user.email "seu.email@gmail.com"

3️⃣ CRIAR REPOSITÓRIO GITHUB:
   a) Acesse: https://github.com/new
   b) Nome: whatsapp-bot-juizado
   c) Descrição: Bot WhatsApp para Juizado Especial
   d) Escolha: Public (para acessar de qualquer lugar)
   e) NÃO marque "Initialize with README"
   f) Clique: "Create repository"

4️⃣ DEPOIS QUE CRIAR, NO TERMINAL:
   
   cd C:\Users\drare\Downloads\Chatbot\drive-download-20260218T165356Z-1-001
   
   git init
   git add .
   git commit -m "Inicial: bot WhatsApp juizado com PM2"
   git branch -M main
   git remote add origin https://github.com/SEU-USUARIO/whatsapp-bot-juizado.git
   git push -u origin main

5️⃣ VERIFICAR RESULTADO:
   Abra no navegador: https://github.com/seu-usuario/whatsapp-bot-juizado
   
   Pronto! Seu projeto está no GitHub!

🔐 IMPORTANTE - ANTES DE FAZER PUSH:
====================================

Certifique-se que:
✓ .wwebjs_auth está em .gitignore (sessão não entra no git)
✓ qrcode.png está em .gitignore (arquivo gerado)
✓ node_modules está em .gitignore (muito grande!)
✓ .env está em .gitignore (dados sensíveis)

Criar .env.example (para referência):
   PORT=3000
   NODE_ENV=production

🔄 APÓS FAZER PUSH:
===================

CLONAR PROJETO EM OUTRO LOCAL:
   git clone https://github.com/seu-usuario/whatsapp-bot-juizado.git
   cd whatsapp-bot-juizado
   npm install
   npm start

📤 FAZER ATUALIZAÇÕES:
======================

Após fazer mudanças locais:
   git add .
   git commit -m "Descrição da mudança"
   git push

Ver histórico:
   git log
   git status

🎯 COMANDOS ÚTEIS:
==================

Criar branch nova para trabalhar:
   git checkout -b feature/nova-funcionalidade

Voltar para main:
   git checkout main

Mesclar branches:
   git merge feature/nova-funcionalidade

Ver remotes:
   git remote -v

⚠️ ERROS COMUNS:
================

"fatal: not a git repository"
→ Execute: git init

"fatal: could not read Username"
→ Use: git config --global user.name "nome"

"Repository not found"
→ Verifique se a URL está correta

"fatal: refusing to merge histories"
→ Use: git pull origin main --allow-unrelated-histories

📌 EXEMPLO COMPLETO (copie e cole):
====================================

git init
git add .
git commit -m "Initial commit: WhatsApp bot with PM2 and Express"
git branch -M main
git remote add origin https://github.com/seu-usuario/whatsapp-bot-juizado.git
git push -u origin main

Pronto! Seu projeto estará no GitHub! 🚀

Para mais informações: https://docs.github.com/pt
