🚀 GUIA DE DEPLOY NO RENDER.COM
=================================

Seu bot agora está pronto para o Render.com!

✅ ARQUIVOS CRIADOS:
====================
✓ Dockerfile - Configuração do container
✓ .dockerignore - Otimização do build

📋 PASSO A PASSO:
==================

1️⃣ FAZER PUSH PARA GITHUB (se ainda não fez):

git init
git add .
git commit -m "Add: Dockerfile para Render.com"
git branch -M main
git remote add origin https://github.com/SEU-USUARIO/whatsapp-bot-juizado.git
git push -u origin main

2️⃣ ACESSAR RENDER.COM:

→ https://render.com
→ Clique em "Sign Up" (ou login se tiver)
→ Conecte sua conta GitHub

3️⃣ CRIAR NOVO SERVIÇO:

→ Clique em "New +" → "Web Service"
→ Conecte seu repositório GitHub
→ Selecione: whatsapp-bot-juizado
→ Clique em "Connect"

4️⃣ CONFIGURAR SERVIÇO:

Nome: whatsapp-bot-juizado
Ambiente: Docker
Plano: Free (ou pago se preferir)
Porta: 3000 (automático)

5️⃣ VARIÁVEIS DE AMBIENTE:

No Render, vá em "Environment":

Add Variable:
  KEY: PORT
  VALUE: 3000

Add Variable:
  KEY: NODE_ENV
  VALUE: production

(Se usar OpenAI no futuro, adicione API_KEY aqui)

6️⃣ DEPLOY:

Clique em "Create Web Service"

O Render vai:
✓ Fazer clone do repo
✓ Construir a imagem Docker
✓ Fazer deploy automaticamente
✓ Manter rodando 24/7

7️⃣ ACESSAR BOT:

Após deploy bem-sucedido, você terá uma URL:
https://whatsapp-bot-juizado.onrender.com

Teste as rotas:
→ https://whatsapp-bot-juizado.onrender.com/health
→ https://whatsapp-bot-juizado.onrender.com/status

🔄 ATUALIZAR CÓDIGO:

Sempre que fizer mudanças:

git add .
git commit -m "Update: descrição"
git push

O Render faz auto-deploy quando você faz push!

📝 PRIMEIRA AUTENTICAÇÃO WHATSAPP:

Quando o bot ligar no Render:
1. Acesse a página do app no Render
2. Vá em "Logs"
3. Procure por: "📲 Escaneie o QR Code abaixo:"
4. Veja o QR Code nos logs
5. Escaneie com seu WhatsApp
6. Bot estará conectado!

⚠️ IMPORTANTE SOBRE SESSÃO:

A pasta .wwebjs_auth está em .gitignore
Cada deploy cria uma nova sessão WhatsApp
Você precisa re-escanear o QR Code
Isso é normal e esperado!

Para reutilizar sessão (avançado):
- Use cloud storage (AWS S3, etc)
- Configure volumes persistentes
- Use banco de dados

💾 PERSISTÊNCIA DE DADOS:

No plano Free do Render:
- Dados são perdidos a cada deploy
- Ideal para desenvolvimento

Para produção:
- Use plano pago (inclui persistent disk)
- Ou integre banco de dados

🔧 TROUBLESHOOTING:

❌ "Build Failed"
→ Verifique se Dockerfile está correto
→ Certifique-se que package.json existe
→ Rode localmente: docker build -t bot .

❌ "Port already in use"
→ O Render atribui porta automaticamente
→ Não se preocupe, vai funcionar

❌ "Deployment crashed"
→ Verifique logs no Render
→ Procure por erros de sintaxe
→ Teste localmente primeiro

❌ "Module not found"
→ Rode: npm install
→ Commit: git add package-lock.json
→ Push: git push

🧪 TESTAR LOCALMENTE (Antes de push):

Instalar Docker (opcional):
→ https://www.docker.com/products/docker-desktop

Testar imagem:

docker build -t whatsapp-bot .
docker run -p 3000:3000 whatsapp-bot

Depois abra:
→ http://localhost:3000/health

📊 MONITORAR BOT:

No Render Dashboard:
✓ Logs em tempo real
✓ Métricas de CPU/Memória
✓ Status de uptime
✓ Histórico de deploys

Para mais info:
→ Clique no nome do app
→ Vá em "Logs"

🔐 SEGURANÇA:

✓ Seu repositório é público (código aberto)
✓ Variáveis sensíveis estão em .env (não no git)
✓ .wwebjs_auth não vai pro GitHub
✓ Nenhuma credencial no código

Se precisar de chaves:
- Defina em "Environment" no Render
- Nunca coloque no código

💰 CUSTOS:

Free Tier:
- 750 horas/mês gratuitas
- Suficiente para 24/7 (mais ou menos)
- Auto-hibernação após 15 min sem uso

Pago:
- $7/mês - Sem hibernação
- $12/mês - Incluindo backup

🎯 FLUXO COMPLETO:

1. Código local → GitHub (git push)
2. GitHub → Render (auto-deploy)
3. Render → Docker build
4. Docker → Container rodando
5. Acesso: seu-app.onrender.com

✨ ANTES DE FAZER DEPLOY:

Checklist:
□ Dockerfile existe?
□ .dockerignore existe?
□ package.json está correto?
□ Código foi commitado?
□ GitHub repository público?
□ Conta Render criada?
□ GitHub conectado em Render?

📞 LINKS ÚTEIS:

Render Docs: https://render.com/docs
Docker Docs: https://docs.docker.com
Node.js Docker: https://nodejs.org/en/docs/guides/nodejs-docker-webapp/

🚀 RESUMO:

1. Fazer push para GitHub
2. Criar Web Service no Render
3. Conectar repositório
4. Deploy automático!
5. Seu bot roda 24/7 na nuvem

Tudo pronto! 🎉
